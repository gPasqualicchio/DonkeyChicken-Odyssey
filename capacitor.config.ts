import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.gpasqualicchio.donkeychicken',
  appName: 'DonkeyChicken Odyssey',
  webDir: 'dist'
};

export default config;
